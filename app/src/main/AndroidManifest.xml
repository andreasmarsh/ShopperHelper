<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.uga.cs.shopperhelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--        android:theme="@style/Theme.ShopperHelper">-->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HelpActivity"
            android:parentActivityName=".MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.MainActivity" />
        </activity>

        <activity android:name=".RegisterActivity"
            android:parentActivityName=".MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.MainActivity" />
        </activity>

        <activity
            android:name=".ShoppingItemManagementActivity"
            android:parentActivityName=".MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.MainActivity" />
        </activity>

        <activity
            android:name=".NewShoppingItemActivity"
            android:parentActivityName=".ReviewShoppingItemsActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.ReviewShoppingItemsActivity" />
        </activity>

        <activity
            android:name=".UpdateItemActivity"
            android:parentActivityName=".ReviewShoppingItemsActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.ReviewShoppingItemsActivity" />
        </activity>

        <activity
            android:name=".PurchaseItemActivity"
            android:parentActivityName=".ReviewShoppingItemsActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.ReviewShoppingItemsActivity" />
        </activity>

        <activity
            android:name=".ReviewShoppingItemsActivity"
            android:parentActivityName=".ShoppingItemManagementActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.ShoppingItemManagementActivity" />
        </activity>

        <activity
            android:name=".ReviewPurchasedItemsActivity"
            android:parentActivityName=".ShoppingItemManagementActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.ShoppingItemManagementActivity" />
        </activity>

        <activity
            android:name=".ModifyItemActivity"
            android:parentActivityName=".ReviewShoppingItemsActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.ReviewShoppingItemsActivity" />
        </activity>

        <activity
            android:name=".ModifyPurchaseActivity"
            android:parentActivityName=".ReviewPurchasedItemsActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.ReviewPurchasedItemsActivity" />
        </activity>

        <activity
            android:name=".UpdatePurchaseActivity"
            android:parentActivityName=".ModifyPurchaseActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.ModifyPurchaseActivity" />
        </activity>

        <activity
            android:name=".SettleCostActivity"
            android:parentActivityName=".ShoppingItemManagementActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="edu.uga.cs.shopperhelper.ShoppingItemManagementActivity" />
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>